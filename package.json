{
  "name": "ai-journal-system",
  "version": "1.0.0",
  "description": "AI-powered journaling system with nature session reflections",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev:frontend": "npm run dev --prefix frontend",
    "start:backend": "npm start --prefix backend",
    "dev": "npx concurrently \"npm run start:backend\" \"npm run dev:frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
